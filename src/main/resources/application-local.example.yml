# 复制为 application-local.yml 后填写本机真实配置。
# application-local.yml 已被 Git 忽略，不会被提交。
spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/ai_agent
    username: postgres
    password: change-me
  ai:
    openai:
      api-key: your-dashscope-api-key

searchAPI:
  api-key: your-search-api-key

minio:
  endpoint: http://127.0.0.1:9000
  access-key: your-minio-access-key
  secret-key: your-minio-secret-key
  bucket-name: rag
